#include <stdio.h>
#include <stdlib.h>

int main()
{
   float a,b,c,d,e,sum,per;
	printf("Enter Marks of the student: ");
	scanf("%f %f %f %f %f",&a,&b,&c,&d,&e);
	sum=a+b+c+d+e;
	per=sum/5;
	printf("\nSum %f",sum);
    printf("\nPercentage: %f",per);
	return 0;
}
